## Creates Time Entry in MS Dataverse

### Install following tools
- VS 2022
- Docker
- Postman (Optional)

### To run Function locally run Azurite in Docker
- Open `local` folder and run `docker-compose up -d`